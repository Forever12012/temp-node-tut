// USED IN 3-Module.js

const items = ['item1', 'item2'];
// module.exports.items = ['item1', 'item2']; We can also directly use modules.exports.

const person = {
    name : 'bob',
}

module.exports.singlePerson = person; // Or we can exports in this way too.